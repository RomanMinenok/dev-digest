# reviewer-core — specs

Design decisions, ADRs, and proposals for `@devdigest/reviewer-core` — the *why*
behind non-obvious choices, and specs for work not yet built.

Put here: one file per decision/feature (e.g. `grounding-gate.md`,
`map-reduce-strategy.md`, `no-io-contract.md`). Reference an accepted spec from
`reviewer-core/CLAUDE.md` when it encodes a convention worth loading on demand.
