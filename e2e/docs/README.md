# e2e — docs

Deeper testing notes for `@devdigest/e2e` that are too detailed for
`e2e/CLAUDE.md` (which is a map, not documentation).

Put here: flow-format reference, runner internals, hermetic-stack setup,
debugging tips. Keep `e2e/README.md` as the overview and the executable specs in
`e2e/specs/*.flow.json`; link from the CLAUDE.md rather than duplicating prose.
