/* ConfigureRun/constants.ts — non-copy magic values for the Configure-run
   screen (SPEC-05, T-24). Copy lives in
   client/messages/en/multiAgent.json ("configure" namespace, T-28). */

export {};
