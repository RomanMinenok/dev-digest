/* ResultsTabs/constants.ts — non-copy magic values for the Tabs results
   view (SPEC-05, T-26). Copy lives in client/messages/en/multiAgent.json
   ("results" namespace, T-28), plus reused `runs.viewTrace` /
   `prReview.finding.*` keys — see ResultsTabs.tsx, AgentSummaryCard.tsx,
   FindingItem.tsx. */

export {};
