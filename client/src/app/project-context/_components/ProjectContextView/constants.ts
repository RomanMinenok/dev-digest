/** Number of skeleton placeholder rows shown while repos/docs are loading. */
export const SKELETON_ROWS = 4;

/** Width of the left list column in the master-detail shell. */
export const LIST_WIDTH = 360;
