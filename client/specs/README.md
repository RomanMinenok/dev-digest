# client — specs

Design decisions, ADRs, and UI proposals for `@devdigest/web` — the *why* behind
non-obvious choices, and specs for work not yet built.

Put here: one file per decision/feature (e.g. `live-log-sse.md`,
`agent-editor-ux.md`). Reference an accepted spec from `client/CLAUDE.md` when it
encodes a convention worth loading on demand.
