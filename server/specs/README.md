# server — specs

Design decisions, ADRs, and proposals for `@devdigest/api` — the *why* behind
non-obvious choices, and specs for work not yet built.

Put here: one file per decision/feature (e.g. `repo-intel-degraded-contract.md`,
`secrets-storage.md`). Reference an accepted spec from `server/CLAUDE.md` when it
encodes a convention worth loading on demand.
