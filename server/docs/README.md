# server — docs

Deeper architecture notes for `@devdigest/api` that are too detailed for
`server/CLAUDE.md` (which is a map, not documentation).

Put here: pipeline walkthroughs, sequence diagrams, data-flow write-ups,
adapter/DI explanations. Keep `server/README.md` as the route-contract index and
link from `server/CLAUDE.md` rather than duplicating prose.
